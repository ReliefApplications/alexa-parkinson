version: '2'
services:

  devapp:
    image: alexa-parkinson
    build: .
    ports:
        - '12118:12118'
    volumes:
        - './:/usr/dockers/devapp/'
    entrypoint: Docker/docker-entrypoint.sh
    restart: always